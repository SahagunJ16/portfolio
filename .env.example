# Absolute origin of the deployed site.
# Used for canonical URLs, the sitemap, robots.txt and Open Graph image URLs.
# No trailing slash. Falls back to http://localhost:3000 when unset.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
